/**
 * @description takes two arguments and adds them together
 * @example addTwo(5,5) // evaluates to 10
 * @param {Number} a
 * @param {Number} b
 * @returns Number
 */
const addTwo = (a, b) => {
  // fill in function logic
};

/**
 * @description takes a string and repeats it count number of times
 * @example repeatString("hi", 3) // evaluates to "hihihi"
 * @param {String} stringToRepeat
 * @param {Number} count
 */
const repeatString = (stringToRepeat, count) => {
  // fill in function logic
};

/**
 * @description takes an array and returns the most common item
 * if there are more than 1, return an array of most common items
 * @example mostCommonItem([1,1,2]) // evaluates to 1
 * @example mostCommonItem([1,2,2,3,3]) // evaluates to [2,3]
 * @param {Any} inputArray
 * @returns {Any || Any[]}
 */
const mostCommonItem = (inputArray) => {
  // fill in function logic
};

/**
 * @description takes a string and check if its palindome;
 * a palindrome is a word that is the same forwards and backwords
 * @example checkPalindrome("aabbaa") // evaluates to true
 * @example checkPalindrome("aab") // evaluates to false
 * @param {String} inputString
 * @returns {Boolean}
 */
const checkPalindrome = (inputString) => {
  // fill in function logic
};

/**
 * @description takes two strings and check if they have all the same letters
 * @example checkAnagram("aabb", "abab") // evaluates to true
 * @example checkAnagram("aabv", "pwmd") // evalutes to false
 * @param {*} baseString
 * @param {*} stringToMatch
 * @returns {Boolean}
 */
const checkAnagram = (baseString, stringToMatch) => {
  // fill in function logic
};

module.exports = {
  addTwo,
  repeatString,
  mostCommonItem,
  checkPalindrome,
  checkAnagram,
};
